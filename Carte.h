
#ifndef Carte_h
#define Carte_h

#include "Arduino.h"

class Carte{
   public:
      Carte(int val, int state);
      //byte myval;
      //byte mystate;
      //boolean AllG;
      //boolean LocG;
      /*
      boolean get_AllGood();
      boolean get_LocGood(); 
      boolean valid();
      boolean* check();

      void update();
      */
};
#endif